# Shared Workspace Assets

This folder contains contracts and documentation shared across the ecommerce ecosystem.

Current contents:
- `contracts/socket-events.json`: canonical realtime event names and room conventions
- `contracts/api-surface.md`: stable public API shape for user and admin clients

This folder intentionally does not contain shared React Native boot code or shared native project files.
